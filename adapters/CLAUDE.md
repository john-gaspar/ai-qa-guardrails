# Adapter: Claude

- Apply `.agent/rules/ai-assistant-rules.md` and `SECURITY.md` for every session.
- Keep prompts concise and structured using `PROMPTS/` templates.
- Use placeholders only; never supply secrets or real hostnames.
